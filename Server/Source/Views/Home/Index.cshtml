@{
    var todos = (List<todoweb.Client.Models.Todo>)ViewData["Todos"];
    <p>You have @todos.Count TODOs:</p>
    <ul>
        @foreach (var todo in todos)
        {
            <li>@todo.Title</li>
        }

    </ul>
}
